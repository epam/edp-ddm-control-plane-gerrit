apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: form-management-provider-db-policy
  namespace: {{ .Values.namespace }}
spec:
  podSelector:
    matchLabels:
      app: form-management-provider-db
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: form-management-provider
        - namespaceSelector:
            matchLabels:
              name: {{ .Values.namespace }}